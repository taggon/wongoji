{
  "name": "@wongoji/counter",
  "module": "src/index.ts",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "test": "bun test",
    "dev": "bun test --watch",
    "build:types": "tsc ./src/index.ts --declaration --emitDeclarationOnly --declarationDir ./dist",
    "build:lib": "bun build ./src/index.ts --target node --minify --sourcemap=external --outdir ./dist",
    "build": "rm -rf dist && bun run build:types && bun run build:lib"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
